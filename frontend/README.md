# Frontend (Vite + React)

This directory is prepared as the new frontend root.

Current scope:
- Tooling and build configuration only.
- No React app code has been created yet.

Expected Cloudflare Pages settings:
- Root directory: `frontend`
- Build command: `npm run build`
- Build output directory: `dist`
