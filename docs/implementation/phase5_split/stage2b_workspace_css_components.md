# Phase5B Stage2B - CSS Components

## 目标
补齐卡片、按钮、列表、输入框等组件样式。

## 强约束（必须执行）
- 任何代码文件不得超过 200 行。
- 每完成一个 checkbox 就立刻打勾。
- 必须严格遵守本文件指令。

## 任务清单
- [x] 在 `frontend/Modules/workspace/style.css` 实现卡片阴影与圆角。
- [x] 实现主按钮与次按钮样式（含 hover/active）。
- [x] 实现历史列表项 hover/active 样式。
- [x] 实现 LLM 浮窗样式与可拖动提示区。

## 交付物（必须有）
- `frontend/Modules/workspace/style.css`

## 提交要求
- [ ] 完成本 stage 后执行 `git add` 涉及文件。
- [ ] 提交命名必须为：`Phase5B Stage2B - CSS Components`（当前 stage 编号）。
- [ ] 确认无任何环境变量文件被提交。
## 自检
- [ ] 文件 < 200 行
