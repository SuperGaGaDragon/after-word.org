# Phase4E Stage4A - LLM API Schemas

## 目标
建立 LLM comment API 的请求/响应 schema。

## 强约束（必须执行）
- 任何代码文件不得超过 200 行。
- 每完成一个 checkbox 就立刻打勾。
- 必须严格遵守本文件指令。

## 任务清单
- [x] 在 `backend/api/llm/schemas.py` 定义 comment 请求/响应 schema。
- [x] 请求字段包含 work_id 与 text_snapshot。

## 交付物（必须有）
- `backend/api/llm/schemas.py`

## 提交要求
- [x] 完成本 stage 后执行 `git add` 涉及文件。
- [x] 提交命名必须为：`Phase4E Stage4A - LLM API Schemas`（当前 stage 编号）。
- [x] 确认无任何环境变量文件被提交。
## 自检
- [x] 文件 < 200 行
