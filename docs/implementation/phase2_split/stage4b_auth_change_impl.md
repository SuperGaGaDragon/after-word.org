# Phase2D Stage4B - Change Credentials Implementation

## 目标
实现改密码与改用户名逻辑。

## 强约束（必须执行）
- 任何代码文件不得超过 200 行。
- 每完成一个 checkbox 就立刻打勾。
- 必须严格遵守本文件指令。

## 任务清单
- [x] 在 `backend/modules/auth/change.py` 实现 change_password/change_username。
- [x] 所有违规规则抛 BusinessError（例如 PASSWORD_MISMATCH / USERNAME_TAKEN）。

## 交付物（必须有）
- `backend/modules/auth/change.py`

## 提交要求
- [x] 完成本 stage 后执行 `git add` 涉及文件。
- [x] 提交命名必须为：`Phase2D Stage4B - Change Credentials Implementation`（当前 stage 编号）。
- [x] 确认无任何环境变量文件被提交。
## 自检
- [x] email 不可修改
- [x] 文件 < 200 行
