# Phase2A Stage1B - Auth Check Implementation

## 目标
实现 Auth Check 函数逻辑。

## 强约束（必须执行）
- 任何代码文件不得超过 200 行。
- 每完成一个 checkbox 就立刻打勾。
- 必须严格遵守本文件指令。

## 任务清单
- [x] 在 `backend/modules/auth/check.py` 实现 is_email_taken/is_username_taken。
- [x] 仅调用 storage 层方法，返回布尔。

## 交付物（必须有）
- `backend/modules/auth/check.py`

## 提交要求
- [x] 完成本 stage 后执行 `git add` 涉及文件。
- [x] 提交命名必须为：`Phase2A Stage1B - Auth Check Implementation`（当前 stage 编号）。
- [x] 确认无任何环境变量文件被提交。
## 自检
- [x] 不抛 BusinessError
- [x] 文件 < 200 行
