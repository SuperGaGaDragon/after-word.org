# Phase2E Stage5B - JWT Implementation

## 目标
实现 JWT 生成与解析工具。

## 强约束（必须执行）
- 任何代码文件不得超过 200 行。
- 每完成一个 checkbox 就立刻打勾。
- 必须严格遵守本文件指令。

## 任务清单
- [x] 在 `backend/modules/auth/jwt.py` 实现 create_token/decode_token。
- [x] 使用 `JWT_SECRET_KEY` 与 `JWT_EXPIRE_MINUTES`。

## 交付物（必须有）
- `backend/modules/auth/jwt.py`

## 提交要求
- [ ] 完成本 stage 后执行 `git add` 涉及文件。
- [ ] 提交命名必须为：`Phase2E Stage5B - JWT Implementation`（当前 stage 编号）。
- [ ] 确认无任何环境变量文件被提交。
## 自检
- [ ] 过期时间 = 60 分钟
- [ ] 文件 < 200 行
