# Phase2C Stage3B - Login Implementation

## 目标
实现登录逻辑：用户查找 + 密码校验。

## 强约束（必须执行）
- 任何代码文件不得超过 200 行。
- 每完成一个 checkbox 就立刻打勾。
- 必须严格遵守本文件指令。

## 任务清单
- [x] 在 `backend/modules/auth/login.py` 实现 login。
- [x] 用户不存在或密码不匹配抛 BusinessError（例如 INVALID_CREDENTIALS）。

## 交付物（必须有）
- `backend/modules/auth/login.py`

## 提交要求
- [x] 完成本 stage 后执行 `git add` 涉及文件。
- [x] 提交命名必须为：`Phase2C Stage3B - Login Implementation`（当前 stage 编号）。
- [x] 确认无任何环境变量文件被提交。
## 自检
- [x] 仅在 Auth 组件处理身份
- [x] 文件 < 200 行
