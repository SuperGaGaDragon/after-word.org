# LLM Gateway Contract

## Function
- generate_comment(text_snapshot) -> comment_text

## Prohibitions
- Accepts only text snapshots, no JWT or user credentials.
- Does not access Storage or change system state.
